#include<iostream>

using namespace std;


int main()
{
	

	// how an elevator handles and keeps track of requests made by users. 
	// all requests users make are stored

	// A user pushes either the up or down button to request the elevator.


	//Once the elevator arrives, the user gets in and specifies his/her destination floor.
	//The elevator prioritizes the requests that are on the way where it’s going, but also based on a first come first served principle

	//Meanwhile, other users outside or inside the elevator make requests as well. The elevator stores the requests and handles them efficiently

	return 0;
}